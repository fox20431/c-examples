#include <stdio.h>

int main() {
    // 定义两个整数
    int a = 5;
    int b = 3;

    // 使用异或进行操作
    int result = a ^ b;

    // 打印操作数和结果
    printf("a: %d\n", a);
    printf("b: %d\n", b);
    printf("a ^ b: %d\n", result);

    return 0;
}
